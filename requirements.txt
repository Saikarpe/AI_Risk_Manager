# Pinned to exact versions used to train and pickle model/model.pkl — a newer
# scikit-learn/xgboost resolved by a >= range can silently break unpickling the
# trained pipeline (this bit us once: deployed /score returned 500s while /health
# and /metrics, which never touch model.pkl, worked fine). If you retrain, re-pin
# to match your new environment.
pandas==2.1.4
numpy==1.26.2
scikit-learn==1.3.2
xgboost==3.0.4
fastapi>=0.110
uvicorn[standard]>=0.27
joblib==1.5.1
faker>=24.0
pydantic>=2.5
